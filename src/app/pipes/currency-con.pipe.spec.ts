import { CurrencyConPipe } from './currency-con.pipe';

describe('CurrencyConPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyConPipe();
    expect(pipe).toBeTruthy();
  });
});
